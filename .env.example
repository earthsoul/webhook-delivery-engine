# Supabase Postgres — use the POOLER URL in Transaction mode (port 6543).
# Supabase Dashboard → Settings → Database → Connection pooling → Transaction.
POSTGRES_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# Bearer token for manual / local worker calls (curl, the e2e harness).
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WORKER_SECRET=replace-me-with-a-long-random-string

# Bearer token Vercel Cron sends automatically on the scheduled worker call.
# Set this in the Vercel project (not needed for local dev). The worker accepts
# a token matching EITHER WORKER_SECRET or CRON_SECRET.
# CRON_SECRET=replace-me-with-another-long-random-string
